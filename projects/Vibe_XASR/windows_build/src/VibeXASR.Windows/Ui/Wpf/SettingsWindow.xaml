<Window x:Class="VibeXASR.Windows.Ui.Wpf.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Vibe XASR 设置" Height="720" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#15151B"
        FontFamily="Segoe UI Variable Text, Segoe UI"
        TextOptions.TextFormattingMode="Ideal" TextOptions.TextRenderingMode="ClearType"
        UseLayoutRounding="True" SnapsToDevicePixels="True">
    <Window.Resources>
        <ResourceDictionary Source="Styles.xaml"/>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="224"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- sidebar -->
        <Border Grid.Column="0" Background="{StaticResource Sidebar}">
            <Border.Effect><DropShadowEffect BlurRadius="22" ShadowDepth="0" Opacity="0.4" Color="#000000"/></Border.Effect>
            <StackPanel Margin="0,18,0,0">
                <StackPanel Orientation="Horizontal" Margin="20,4,0,16">
                    <Border Width="32" Height="32" CornerRadius="9" Background="{StaticResource AccentGrad}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Rectangle Width="3" Height="9"  Fill="White" RadiusX="1.5" RadiusY="1.5" Margin="1,0"/>
                            <Rectangle Width="3" Height="15" Fill="White" RadiusX="1.5" RadiusY="1.5" Margin="1,0"/>
                            <Rectangle Width="3" Height="11" Fill="White" RadiusX="1.5" RadiusY="1.5" Margin="1,0"/>
                        </StackPanel>
                    </Border>
                    <TextBlock Text="Vibe XASR" Foreground="{StaticResource Text}" FontSize="15.5" FontWeight="SemiBold" VerticalAlignment="Center" Margin="11,0,0,0"/>
                </StackPanel>
                <StackPanel x:Name="Nav"/>
            </StackPanel>
        </Border>

        <!-- content -->
        <Grid Grid.Column="1">
            <ScrollViewer x:Name="ContentScroll" VerticalScrollBarVisibility="Auto" Padding="0,8,0,24">
                <StackPanel x:Name="Content" MaxWidth="720" HorizontalAlignment="Stretch"/>
            </ScrollViewer>
            <!-- full-bleed host (记录 workspace): replaces the scroller for tabs that need the whole pane -->
            <ContentControl x:Name="FullBleed" Visibility="Collapsed"/>
        </Grid>
    </Grid>
</Window>
