<Window x:Class="VibeXASR.Windows.Ui.Wpf.PreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Vibe XASR 设置" Height="660" Width="880"
        WindowStartupLocation="CenterScreen"
        Background="#15151B"
        FontFamily="Segoe UI Variable Text, Segoe UI"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="ClearType"
        UseLayoutRounding="True" SnapsToDevicePixels="True">

    <Window.Resources>
        <!-- ===== palette (macOS DesignTokens) ===== -->
        <SolidColorBrush x:Key="Text"        Color="#ECECF1"/>
        <SolidColorBrush x:Key="TextMuted"   Color="#8A8A99"/>
        <SolidColorBrush x:Key="Surface2"    Color="#1E1E26"/>
        <SolidColorBrush x:Key="Field"       Color="#16161C"/>
        <SolidColorBrush x:Key="Hairline"    Color="#1AFFFFFF"/>
        <SolidColorBrush x:Key="AccentA"     Color="#7C5CFF"/>
        <SolidColorBrush x:Key="AccentB"     Color="#38E1D6"/>
        <SolidColorBrush x:Key="AccentSoft"  Color="#337C5CFF"/>
        <SolidColorBrush x:Key="Hover"       Color="#14FFFFFF"/>
        <LinearGradientBrush x:Key="AccentGrad" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#7C5CFF" Offset="0"/>
            <GradientStop Color="#38E1D6" Offset="1"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="Sidebar" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#1A1A22" Offset="0"/>
            <GradientStop Color="#141419" Offset="1"/>
        </LinearGradientBrush>

        <!-- ===== card ===== -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource Surface2}"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="BorderBrush" Value="{StaticResource Hairline}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="26" ShadowDepth="4" Opacity="0.32" Color="#000000" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===== nav item (pill) ===== -->
        <Style x:Key="NavItem" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Margin" Value="10,1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="GroupName" Value="nav"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="bd" CornerRadius="10" Background="Transparent" Padding="14,0">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{StaticResource Hover}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{StaticResource AccentSoft}"/>
                                <Setter Property="Foreground" Value="{StaticResource Text}"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===== toggle ===== -->
        <Style x:Key="Toggle" TargetType="ToggleButton">
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="track" CornerRadius="14" Background="#2EFFFFFF">
                            <Border x:Name="thumb" Width="22" Height="22" CornerRadius="11" Background="White"
                                    HorizontalAlignment="Left" Margin="3,0,0,0">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="6" ShadowDepth="1" Opacity="0.35" Color="#000000"/>
                                </Border.Effect>
                            </Border>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="track" Property="Background" Value="{StaticResource AccentGrad}"/>
                                <Setter TargetName="thumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="thumb" Property="Margin" Value="0,0,3,0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===== thin macOS-style scrollbar (applies to all ScrollBars) ===== -->
        <Style TargetType="Thumb" x:Key="SbThumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border CornerRadius="3.5" Background="#4DFFFFFF" Width="7" HorizontalAlignment="Center"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ScrollBar">
            <Setter Property="Width" Value="11"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="Transparent">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource SbThumb}"/>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="False"/>
                                </Track.IncreaseRepeatButton>
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="False"/>
                                </Track.DecreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===== a settings row (title + desc + control) ===== -->
        <Style x:Key="RowText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style x:Key="RowDesc" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
            <Setter Property="FontSize" Value="11.5"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,3,0,0"/>
        </Style>
        <Style x:Key="GroupTitle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="6,18,0,8"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="224"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ===== sidebar ===== -->
        <Border Grid.Column="0" Background="{StaticResource Sidebar}">
            <Border.Effect><DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.4" Color="#000000"/></Border.Effect>
            <StackPanel Margin="0,18,0,0">
                <!-- logo + name -->
                <StackPanel Orientation="Horizontal" Margin="20,4,0,18">
                    <Border Width="32" Height="32" CornerRadius="9" Background="{StaticResource AccentGrad}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Rectangle Width="3" Height="9"  Fill="White" RadiusX="1.5" RadiusY="1.5" Margin="1,0"/>
                            <Rectangle Width="3" Height="15" Fill="White" RadiusX="1.5" RadiusY="1.5" Margin="1,0"/>
                            <Rectangle Width="3" Height="11" Fill="White" RadiusX="1.5" RadiusY="1.5" Margin="1,0"/>
                        </StackPanel>
                    </Border>
                    <TextBlock Text="Vibe XASR" Foreground="{StaticResource Text}" FontSize="15.5" FontWeight="SemiBold"
                               VerticalAlignment="Center" Margin="11,0,0,0"/>
                </StackPanel>

                <RadioButton Style="{StaticResource NavItem}" Content="⚙   通用"/>
                <RadioButton Style="{StaticResource NavItem}" Content="🎙   听写" IsChecked="True"/>
                <RadioButton Style="{StaticResource NavItem}" Content="📖   词典"/>
                <RadioButton Style="{StaticResource NavItem}" Content="⚡   口令"/>
                <RadioButton Style="{StaticResource NavItem}" Content="🧠   模型"/>
                <RadioButton Style="{StaticResource NavItem}" Content="🔗   共享"/>
                <RadioButton Style="{StaticResource NavItem}" Content="✨   AI 润色"/>
                <RadioButton Style="{StaticResource NavItem}" Content="🗒   记录"/>
                <RadioButton Style="{StaticResource NavItem}" Content="🔐   权限"/>
                <RadioButton Style="{StaticResource NavItem}" Content="ⓘ   关于"/>
            </StackPanel>
        </Border>

        <!-- ===== content ===== -->
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" Padding="28,8,16,20">
            <StackPanel MaxWidth="560" HorizontalAlignment="Left">

                <TextBlock Style="{StaticResource GroupTitle}" Text="听写"/>

                <!-- hotkey card -->
                <Border Style="{StaticResource Card}" Margin="0,0,0,12">
                    <Grid Margin="18,14">
                        <StackPanel>
                            <TextBlock Style="{StaticResource RowText}" Text="快捷键"/>
                            <TextBlock Style="{StaticResource RowDesc}" Text="按住它说话,松开即把识别结果插入光标处。"/>
                        </StackPanel>
                        <Border HorizontalAlignment="Right" VerticalAlignment="Center" Background="{StaticResource Field}"
                                CornerRadius="8" BorderBrush="{StaticResource Hairline}" BorderThickness="1" Padding="16,7">
                            <TextBlock Text="Right Ctrl" Foreground="{StaticResource Text}" FontFamily="Cascadia Mono, Consolas" FontSize="12.5"/>
                        </Border>
                    </Grid>
                </Border>

                <!-- mode cards -->
                <Border Style="{StaticResource Card}" Margin="0,0,0,12">
                    <StackPanel Margin="16,14">
                        <TextBlock Style="{StaticResource RowText}" Text="听写模式" FontWeight="SemiBold" Margin="2,0,0,10"/>
                        <Border CornerRadius="10" Background="#337C5CFF" BorderBrush="{StaticResource AccentA}" BorderThickness="1.4" Padding="14,11" Margin="0,0,0,8">
                            <Grid>
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="说完插入" Foreground="{StaticResource Text}" FontSize="13" FontWeight="SemiBold"/>
                                        <Border CornerRadius="7" Background="#337C5CFF" Margin="8,0,0,0" Padding="7,1">
                                            <TextBlock Text="推荐" Foreground="{StaticResource AccentA}" FontSize="10" FontWeight="SemiBold"/>
                                        </Border>
                                    </StackPanel>
                                    <TextBlock Style="{StaticResource RowDesc}" Text="说完一句、松开快捷键后一次性插入,最稳。"/>
                                </StackPanel>
                                <Ellipse Width="18" Height="18" Stroke="{StaticResource AccentA}" StrokeThickness="5" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <Border CornerRadius="10" Background="{StaticResource Field}" BorderBrush="{StaticResource Hairline}" BorderThickness="1" Padding="14,11" Margin="0,0,0,8">
                            <Grid>
                                <StackPanel>
                                    <TextBlock Text="逐字上屏" Foreground="{StaticResource Text}" FontSize="13"/>
                                    <TextBlock Style="{StaticResource RowDesc}" Text="边说边上屏,像打字一样实时出现。"/>
                                </StackPanel>
                                <Ellipse Width="18" Height="18" Stroke="{StaticResource Hairline}" StrokeThickness="1.5" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <Border CornerRadius="10" Background="{StaticResource Field}" BorderBrush="{StaticResource Hairline}" BorderThickness="1" Padding="14,11">
                            <Grid>
                                <StackPanel>
                                    <TextBlock Text="持续候机" Foreground="{StaticResource Text}" FontSize="13"/>
                                    <TextBlock Style="{StaticResource RowDesc}" Text="常开监听,适合长时间口述。"/>
                                </StackPanel>
                                <Ellipse Width="18" Height="18" Stroke="{StaticResource Hairline}" StrokeThickness="1.5" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- toggles card -->
                <Border Style="{StaticResource Card}">
                    <StackPanel Margin="18,6">
                        <Grid Height="56">
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Style="{StaticResource RowText}" Text="数字规整"/>
                                <TextBlock Style="{StaticResource RowDesc}" Text="把口语数字转成阿拉伯数字(一百二十三 → 123)。"/>
                            </StackPanel>
                            <ToggleButton Style="{StaticResource Toggle}" IsChecked="True" HorizontalAlignment="Right"/>
                        </Grid>
                        <Border Height="1" Background="{StaticResource Hairline}"/>
                        <Grid Height="56">
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Style="{StaticResource RowText}" Text="去口水词"/>
                                <TextBlock Style="{StaticResource RowDesc}" Text="删掉「嗯 / 呃 / 唉」等语气词与口吃重复。"/>
                            </StackPanel>
                            <ToggleButton Style="{StaticResource Toggle}" IsChecked="True" HorizontalAlignment="Right"/>
                        </Grid>
                        <Border Height="1" Background="{StaticResource Hairline}"/>
                        <Grid Height="56">
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Style="{StaticResource RowText}" Text="提示音"/>
                                <TextBlock Style="{StaticResource RowDesc}" Text="说话起止播放轻提示音。"/>
                            </StackPanel>
                            <ToggleButton Style="{StaticResource Toggle}" HorizontalAlignment="Right"/>
                        </Grid>
                        <Border Height="1" Background="{StaticResource Hairline}"/>
                        <Grid Height="56">
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Style="{StaticResource RowText}" Text="保存到记录"/>
                                <TextBlock Style="{StaticResource RowDesc}" Text="把每次识别结果存进「记录」,可随时回看。"/>
                            </StackPanel>
                            <ToggleButton Style="{StaticResource Toggle}" IsChecked="True" HorizontalAlignment="Right"/>
                        </Grid>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
